# tool_cm

Standalone extraction from `workspace_archlinux_i3wm`.

## Files

- `bin/cm` from workspace `bin/cm`

## Install

```bash
./install.sh
```

By default commands from `bin/` are installed to `$HOME/.local/bin`.
Override with `PREFIX=/path ./install.sh`.
